<%
  sizeClass = defined?(size) ? "c-pulser--#{size}" : ""
  classes = classes || ''
%>

<div class="c-pulser  <%= sizeClass %>  <%= classes %>">
  <span class="c-pulser__element"></span>
  <span class="c-pulser__element"></span>
  <span class="c-pulser__element"></span>
</div>
