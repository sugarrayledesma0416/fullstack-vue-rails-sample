VHL = VHL || {};
VHL.Activity = VHL.Activity || {};

VHL.Activity = {
  is_storing_work: function() {
  //is_storing_work is defined in mae protect_work_feature.js. This does not have to be in the mae 
  //Once all the reference to is_storing_work method is replaced by VHL.Activity.is_storing_work , it can be removed.
  //All m3 only activity js can be brought under VHL.Activity
    is_storing_work();
  },
}
