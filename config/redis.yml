default:
  host: localhost
  port: 6379

development:
  host: localhost
  port: 6379

test:
  host: localhost
  port: 6379

production:
  host: localhost
  port: 6379

# The values for staging and live are configured by chef. Just need the
# empty hash for live here so env doesn't die when compiling local assets.
live: {}

staging: {}

qa: {}
